# Dev Center - Claude 작업 지침

## Hermes / Codex / Claude 협업 규칙

### Claude 작업 시작 조건
- Codex 검토 완료 표시(`[APPROVED]`)가 없는 Hermes 카드는 바로 작업하지 않고 확인 요청
- 아래 정보가 없으면 작업 전 요청할 것:
  - 변경 대상 파일 경로 (명시적)
  - 허용된 변경 유형 (추가/수정/삭제)
  - 변경 금지 항목
  - 검증 기준

### Codex → Claude 핸드오프 프롬프트 형식

```
Task: [한 줄 요약]

Target files:
- path/to/file.php  (수정)
- path/to/other.js  (읽기 전용, 참고용)

Allowed changes:
- [구체적 변경 내용]

Do NOT change:
- [금지 항목]

Validation:
- [완료 기준]

Risk flags: [없음 / DB변경 / 인증 관련 / 결제 관련]
```

### Claude 속도 향상 규칙 (확인 없이 바로 작업 가능한 조건)
- 파일 경로가 명시된 경우 → 탐색 없이 바로 시작
- `Risk flags: 없음` 명시된 경우 → DB/인증/결제 확인 생략
- 검증 기준이 체크리스트 형식인 경우 → 항목별 체크 후 보고

### Claude 작업 후 보고 형식
```
완료 보고:
- 변경된 파일: [목록]
- 변경하지 않은 파일: [범위 내였으나 건드리지 않은 것]
- 검증 결과: [기준별 통과/실패]
- 범위 외 발견 사항: [있으면 메모, 수정은 하지 않음]
- 확인 필요 항목: [있으면 명시]
```

### 금지 사항 (Hermes 카드 내용과 무관하게 항상 적용)
- 범위 밖 파일 수정 → 발견 시 보고만
- 보안 약화 변경 (인증 우회, 권한 완화 등)
- DB 스키마 변경
- 인증/결제 로직 변경
- 위 4가지는 명시적 사용자 확인 없이 절대 진행 안 함

### 범위 확대 기준
- 원래 태스크 카드 대비 변경 파일 수 +1 이상이면 진행 전 사용자 확인
