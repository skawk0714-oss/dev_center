## 7. 리서치/분석 특화 규칙

- 자료 출처를 명시한다. 근거 없는 수치나 주장을 작성하지 않는다.
- 데이터 파일은 `data/` 폴더에 날짜 접두사(`YYYYMMDD_이름`)로 저장한다.
- 분석 결과는 `docs/` 폴더에 마크다운으로 정리한다.
- 코드가 포함된 경우 Python/R 스크립트는 재현 가능하도록 작성한다.
- API 키나 토큰은 `.env` 에만 보관하고 커밋하지 않는다.

### 초기 작업
{{INITIAL_TASK}}
