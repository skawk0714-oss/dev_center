## 7. PHP 로컬 도구 특화 규칙

- 이 도구는 **로컬 전용** 이다. 외부 접속 보안 가드를 반드시 유지한다.
- REMOTE_ADDR 가 127.0.0.1 / ::1 이 아니면 실행 기능을 차단한다.
- 민감 기능에는 CSRF 토큰(세션 기반)을 적용한다.
- 파일 경로는 `realpath()` 로 정규화하고, 허용된 루트 밖은 거부한다.
- 외부 라이브러리 없이 PHP 기본 기능만 사용한다.
- PHP 8.x, 들여쓰기 4칸(스페이스).

### 초기 작업
{{INITIAL_TASK}}
